---
cops_mltrainer_name: mltrainerinstance
cops_mltrainer_project_module_name: "{{cops_mltrainer_name}}"
cops_mltrainer_settings_module: "{{cops_mltrainer_project_module_name}}.settings.instances.{{cops_mltrainer_deploy_env}}"
cops_mltrainer_project_dir: "./src/{{cops_mltrainer_project_module_name.replace('.', '/')}}"
cops_mltrainer_front_dir: "{{cops_mltrainer_project_dir}}"
cops_mltrainer_py_ver: "3.7"
cops_mltrainer_default_deploy_env: "dev"
cops_mltrainer_default_docker_tag: "latest"
cops_mltrainer_default_gitref: "master"
cops_mltrainer_branch_envs: {}
cops_mltrainer_deploy_env: "{{ (
  cops_mltrainer_gitref in [cops_mltrainer_default_gitref, cops_mltrainer_default_docker_tag]
    and cops_mltrainer_default_deploy_env
    or  cops_mltrainer_branch_envs.get(cops_mltrainer_gitref, cops_mltrainer_gitref))}}"
cops_mltrainer_docker_tag: "{{cops_mltrainer_gitref.replace('master', cops_mltrainer_default_docker_tag)}}"
cops_mltrainer_docker_image_tag: "{{cops_mltrainer_name}}"
cops_mltrainer_tld_domain: example.com
cops_mltrainer_mail_domain: "{{ cops_mltrainer_tld_domain }}"
cops_mltrainer_gitref: "{{cops_mltrainer_default_gitref}}"
cops_mltrainer_dir: "/srv/docker/{{cops_mltrainer_name}}"
cops_mltrainer_hostname: "{{cops_mltrainer_deploy_env}}-{{cops_mltrainer_name}}.{{cops_mltrainer_tld_domain}}"
cops_mltrainer_alternate_hostnames: [".{{cops_mltrainer_tld_domain}}"]
cops_mltrainer_secret_key: "SuperSecret"
cops_mltrainer_gateway: "{{ ansible_default_ipv4.address}}"
cops_mltrainer_composename: "{{(ansible_connection|default('ssh') in ['local'] and
  (teleport_wd|copsf_basename) or
  cops_mltrainer_name)}}"
cops_mltrainer_servicename: "{{cops_mltrainer_composename}}"
# docker compose settings (may include ip ip:port)
cops_mltrainer_http_port: 80
cops_mltrainer_https_port: 443
cops_mltrainer_api_port: 8000
cops_mltrainer_api_listen: 127.0.0.1
cops_mltrainer_http_listen: 0.0.0.0
#
cops_mltrainer_db_host: "db"
cops_mltrainer_db_port: "5432"
cops_mltrainer_db_user: "secretJFCxwPHz99R9jIi"
cops_mltrainer_db_password: "secret4gzQNe3c8851VOJ"
cops_mltrainer_db_name: "{{cops_mltrainer_name}}"
cops_mltrainer_devmode: False
cops_mltrainer_build_args: "--no-cache mltrainer cron"
# backup could be remove in a-while when we have redeployed it everywhere
cops_mltrainer_restarted_services_default: "db redis mltrainer nginx backup cron"
cops_mltrainer_restarted_services: "{{cops_mltrainer_restarted_services_default}}"
cops_mltrainer_purge_volumes: [statics]
cops_mltrainer_medias_owner: "1000"
cops_mltrainer_medias_group: "1000"
cops_mltrainer_force_sync_ssh_user: null
cops_mltrainer_sync_ssh_user: ""
cops_mltrainer_sshargs: >-
  {% set k = hostvars[inventory_hostname].get(
                'ansible_ssh_private_key_file', '') %}
  {% if k%}-o IdentityFile={{k}} -o IdentitiesOnly=yes{%endif%}
  {% if (
      hostvars[inventory_hostname].ansible_connection|default('ssh')
     ) not in [ 'smart', 'local'] %}
  {{ hostvars[inventory_hostname].ssh_args | default('') }}
  {{ hostvars[inventory_hostname].ansible_ssh_args | default('') }}
  {{ hostvars[inventory_hostname].ansible_ssh_common_args | default('') }}
  {{ hostvars[inventory_hostname].ansible_ssh_extra_args | default('') }}
  {% endif %}
  {% if((cops_mltrainer_force_sync_ssh_user) or
        (cops_mltrainer_sync_ssh_user and (cops_mltrainer_force_sync_ssh_user == True))
    )%}
  {% set user = cops_mltrainer_sync_ssh_user or ansible_user or '' %}
  {% if user %}-l {{user}}{% endif %}
  {% endif %}
cops_mltrainer_build: false
cops_mltrainer_pull: true
cops_mltrainer_registries: []
#- {registry: registry.foo.net, username: foo, password: bar}
cops_mltrainer_compose_files_prod: [docker-compose.yml, docker-compose-prod.yml]
cops_mltrainer_compose_files_dev: [docker-compose.yml, docker-compose-dev.yml]
cops_mltrainer_compose_files: "{{(ansible_connection|default('ssh') in ['local'] and
  cops_mltrainer_compose_files_dev or cops_mltrainer_compose_files_prod)}}"
cops_mltrainer_dc:
  docker-compose {% for i in cops_mltrainer_compose_files %} -f {{i}}{% endfor %}
  -p "{{cops_mltrainer_composename}}"
cops_mltrainer_default_from_email: noreply@{{cops_mltrainer_hostname}}
cops_mltrainer_server_email: "{{cops_mltrainer_default_from_email}}"
cops_mltrainer_mailcatcher_user: mailcatcher
cops_mltrainer_mailcatcher_password: mailcatcher
cops_mltrainer_email_server: mailcatcher
cops_mltrainer_email_host_user: ''
cops_mltrainer_email_host_password: ''
cops_mltrainer_email_host_port: 1025
cops_mltrainer_email_use_tls: 'no'
cops_mltrainer_http_protect_user: "root"
cops_mltrainer_http_protect_password: "rootroot"
cops_mltrainer_redis_url: "redis://redis:6379/1"
cops_mltrainer_cors_origin_allow_all: "{{cops_mltrainer_devmode and 'y' or 'n' }}"
cops_mltrainer_hosts_whitelist: "{{(
    [cops_mltrainer_hostname, '0.0.0.0', '.{0}'.format(cops_mltrainer_hostname)]+
    cops_mltrainer_alternate_hostnames
  )|join('-|_')}}"
cops_mltrainer_hostnames: "{{cops_mltrainer_hosts_whitelist}}"
cops_mltrainer_sentry_tags: ["env:{{cops_mltrainer_deploy_env}}"]
cops_mltrainer_sentry_dsn: ''
cops_mltrainer_docker_env_freeform: |-
  {{ cops_mltrainer_docker_env_defaults}}
  {{ cops_mltrainer_docker_env_extra}}
cops_mltrainer_gunicorn_class: "sync"
cops_mltrainer_gunicorn_extra_args: ""
cops_mltrainer_generic_db_mode: postgres
cops_mltrainer_db_mode: "{{cops_mltrainer_generic_db_mode}}"
cops_mltrainer_docker_env_defaults: ""
# free form for params inside dockerenv
cops_mltrainer_dotenv_defaults: |-
  # cors
  MLTRAINER__CORS_ORIGIN_WHITELIST={{cops_mltrainer_hosts_whitelist}}
  MLTRAINER__CORS_ORIGIN_ALLOW_ALL={{cops_mltrainer_cors_origin_allow_all}}
  MLTRAINER__COPS_ALL_HOSTNAMES={{cops_mltrainer_hostnames}}
  {% if cops_mltrainer_devmode %}
  MLTRAINER__DEBUG=y
  {% endif %}
  MLTRAINER__DEFAULT_FROM_EMAIL={{ cops_mltrainer_default_from_email }}
  MLTRAINER__SERVER_EMAIL={{ cops_mltrainer_server_email }}
  MLTRAINER__EMAIL_HOST={{cops_mltrainer_email_server}}
  {% if "{0}".format(cops_mltrainer_email_use_tls).lower() in ['yes', 'no'] %}
  MLTRAINER__EMAIL_USE_TLS={{cops_mltrainer_email_use_tls}}
  {% endif %}
  MLTRAINER__EMAIL_PORT={{cops_mltrainer_email_host_port}}
  MLTRAINER__EMAIL_HOST_USER={{cops_mltrainer_email_host_user}}
  {% if cops_mltrainer_email_host_password %}
  MLTRAINER__EMAIL_HOST_PASSWORD={{cops_mltrainer_email_host_password}}
  {% endif %}
  MLTRAINER__REDIS_URL={{cops_mltrainer_redis_url}}
  MLTRAINER__ALLOWED_HOSTS={{cops_mltrainer_hosts_whitelist}}
  MLTRAINER__DEPLOY_ENV={{cops_mltrainer_deploy_env}}
  MLTRAINER__HTTP_PROTECT_USER={{cops_mltrainer_http_protect_user}}
  MLTRAINER__HTTP_PROTECT_PASSWORD={{cops_mltrainer_http_protect_password}}
  {% if cops_mltrainer_sentry_dsn %}
  MLTRAINER__SENTRY_DSN={{cops_mltrainer_sentry_dsn}}
  MLTRAINER__SENTRY_RELEASE={{cops_mltrainer_docker_tag}}
  MLTRAINER__SENTRY_TAGS={{cops_mltrainer_sentry_tags|join(',')}}
  {% endif %}
  # gunicorn
  GUNICORN_CLASS={{cops_mltrainer_gunicorn_class}}
  GUNICORN_EXTRA_ARGS={{cops_mltrainer_gunicorn_extra_args}}
  # This need to be changed in production
  SECRET_KEY={{ cops_mltrainer_secret_key }}
  MAILCATCHER_USER={{cops_mltrainer_mailcatcher_user}}
  MAILCATCHER_PASSWORD={{cops_mltrainer_mailcatcher_password}}
  REDIS_URL={{cops_mltrainer_redis_url}}
  # db
  POSTGRES_HOST={{ cops_mltrainer_db_host }}
  POSTGRES_PORT={{ cops_mltrainer_db_port }}
  POSTGRES_USER={{ cops_mltrainer_db_user }}
  POSTGRES_DB={{ cops_mltrainer_db_name }}
  POSTGRES_PASSWORD={{cops_mltrainer_db_password }}
  PGPASSWD={{cops_mltrainer_db_password }}
  PGPASSWORD={{cops_mltrainer_db_password }}
  MLTRAINER__HTTP_LISTEN={{cops_mltrainer_http_listen}}
  MLTRAINER__API_LISTEN={{cops_mltrainer_api_listen}}
  MLTRAINER__HTTP_PORT={{cops_mltrainer_http_port}}
  MLTRAINER__HTTPS_PORT={{cops_mltrainer_https_port}}
  MLTRAINER__API_PORT={{cops_mltrainer_api_port}}
  APP_TYPE=mltrainer
  MLTRAINER_SETTINGS_MODULE={{cops_mltrainer_settings_module}}
  MLTRAINER_PY_VER={{ cops_mltrainer_py_ver }}
  MLTRAINER_NAME={{ cops_mltrainer_name }}
  COMPOSE_PROJECT_NAME={{cops_mltrainer_composename}}
  CI_COMMIT_SHA={{cops_mltrainer_gitref}}
  MLTRAINER_IMAGE={{ cops_mltrainer_docker_image }}
  MLTRAINER_IMAGE_VERSION={{cops_mltrainer_docker_tag}}
  CONTROL_COMPOSE_FILES={{cops_mltrainer_compose_files|join(' ')}}
cops_mltrainer_dotenv: |
  {{cops_mltrainer_dotenv_defaults}}
  {{cops_mltrainer_dotenv_extra}}
# free form for params inside dockerenv
cops_mltrainer_docker_env_extra: ""
# free form for params inside local.py
cops_mltrainer_localpy_extra: ""
# Normally playbook is in ./.ansible/playbooks
cops_mltrainer_code_dir: "{{playbook_dir}}/../.."
cops_mltrainer_theme_rsync_opts: ["-azv", "--delete"]
cops_mltrainer_code_rsync_opts: [
    "-azv",
    # include deploy projects base code
    "--include", "local/*-deploy",
    "--exclude", ".ansible",
    "--exclude", "local/*",
    "--exclude", ".tox",
    "--exclude", ".env",
    "--exclude", "docker.env"]
cops_mltrainer_backup_postgresql: "{{'5432' == '{0}'.format(cops_mltrainer_db_port)}}"
cops_mltrainer_backup_paths_to_backup:
- "{{cops_mltrainer_dir}}"
cops_mltrainer_backup_docker_volumes: [mediafiles]
cops_mltrainer_backup_docker_volumes_: |-
  {% set res = [] %}
  {% for i in cops_mltrainer_backup_docker_volumes %}
  {% set  _ = res.append('_'.join([cops_mltrainer_composename, i])) %}
  {% endfor %}
  {{ res | to_json }}
cops_mltrainer_backup_pgdump_urls:
- name: "{{cops_mltrainer_db_name}}"
  url: "postgres://{{
      cops_mltrainer_db_user}}:{{cops_mltrainer_db_password}}@{{
        cops_mltrainer_db_host}}:{{cops_mltrainer_db_port}}/{{
          cops_mltrainer_db_name}}"
  docker_dir: "{{cops_mltrainer_dir}}"
  docker_service: backup
  docker_compose: "{{cops_mltrainer_dc}}"
cops_mltrainer_sync_theme_contents_front: |-
  {% set res = {} %}
  {% set _ = res.update({
    "{0}/{1}".format(cops_mltrainer_front_dir, '/static/dist/'): {},
    "{0}/{1}".format(cops_mltrainer_front_dir, '/static/js/'): {},
    "{0}/{1}".format(cops_mltrainer_front_dir, '/static/css/'): {},
    "{0}/{1}".format(cops_mltrainer_front_dir, '/static/styles/'): {},
    "{0}/{1}".format(cops_mltrainer_front_dir, '/static/scripts/'): {},
  }) %}
  {{ res | to_json }}
cops_mltrainer_sync_theme_contents_webpack: |-
  {% set res = {} %}
  {% set _ = res.update({
    "{0}/{1}".format(cops_mltrainer_front_dir, '/webpack-stats.json'): {},
    'webpack-stats.json': {},
  }) %}
  {% set _ = res.update(cops_mltrainer_sync_theme_contents_front) %}
  {{ res | to_json }}
cops_mltrainer_sync_theme_contents: {}
cops_mltrainer_drun_extra_args: "--entrypoint /init.sh"
cops_mltrainer_drun:
  COMPOSE_INTERACTIVE_NO_CLI= {{teleport_dc}}
  run -e SHELL_USER=mltrainer --no-deps
  --rm {{cops_mltrainer_drun_extra_args}} {{teleport_app_container}}
#
# teleport vars (duplicate environment to another)
#
teleport_sshcmd: ssh
teleport_rsynccmd: rsync
teleport_mode: default
# handle retrocompatibility within teleport modes
teleport_modes_mapper: {standard: default}
_teleport_mode: "{{teleport_modes_mapper.get(teleport_mode, teleport_mode)}}"
teleport_reset_perms: true
teleport_db_mode: "{{cops_mltrainer_generic_db_mode}}"
teleport_ms_project: "{{cops_mltrainer_name}}"
teleport_services: [cron, "{{teleport_app_container}}", nginx]
teleport_stop_services: "{{teleport_services}}"
teleport_restart_services: "{{teleport_services}}"
# teleport_mode: default|makinastates
# teleport_origin: <origin from where we restore>
# teleport_destination: <destination to restore>
teleport_wd: "{{(
  ansible_connection|default('ssh') in ['local'] and
  (playbook_dir|copsf_dirname|copsf_dirname) or
  cops_mltrainer_dir)}}"
teleport_orig_wd: "{{cops_mltrainer_dir}}"
teleport_orig_composename: "{{cops_mltrainer_name}}"
teleport_data_dir: "{{teleport_wd}}/local/nobackup/teleport"
teleport_orig_data_dir: "{{teleport_orig_wd}}/local/nobackup/teleport"
teleport_paths_dest_dump_mysql: "/var/lib/mysql/dbdumps"
teleport_paths_dest_dump_postgres: "/var/lib/postgresql/data/dbdumps"
teleport_paths_origin_dump: "{{teleport_orig_data_dir}}/dumps"
teleport_paths_dump: "{{teleport_wd}}/dumps"
teleport_paths_ms_origin_dump: "/srv/projects/{{teleport_ms_project}}/data/nobackup/teleport/dumps"
teleport_ms_medias_dir: "/srv/projects/{{teleport_ms_project}}/data/media"
teleport_db_container: db
teleport_docker_medias_dir: "/var/lib/docker/volumes/{{teleport_orig_composename}}_mediafiles/_data"
teleport_container_medias_dir: /code/public/media
teleport_app_container: mltrainer
teleport_paths_makinastates:
  origin_dump: "{{teleport_paths_ms_origin_dump}}"
  origin_medias: "{{teleport_ms_medias_dir}}"
teleport_paths_default:
  origin_dump: "{{teleport_paths_origin_dump}}"
  origin_medias: "{{teleport_docker_medias_dir}}"
teleport_paths_common:
  dest_dump: "{{lookup('vars', 'teleport_paths_dest_dump_'+teleport_db_mode)}}"
  dest_medias: "{{teleport_container_medias_dir}}"
  dump_file: dump.gz
teleport_paths: "{{teleport_paths_common|combine(lookup('vars', 'teleport_paths_'+_teleport_mode))}}"
teleport_paths_origin_dump_file: "{{teleport_paths.origin_dump}}/{{teleport_paths.dump_file}}"
teleport_paths_dest_dump_file: "{{teleport_paths.dest_dump}}/{{teleport_paths.dump_file}}"
teleport_rsync_args: "-azv --delete --delete-after"
teleport_rsync_extra_args: ""
teleport_destination_ansible_host: "{{groups[teleport_destination][0]}}"
teleport_origin_ansible_host: "{{groups[teleport_origin][0]}}"
teleport_destination_host: "{{hostvars[teleport_destination_ansible_host]['ansible_host'] or teleport_destination_ansible_host}}"
teleport_origin_host: "{{hostvars[teleport_origin_ansible_host].get('ansible_host', teleport_origin_ansible_host)}}"
teleport_dc: "{{cops_mltrainer_dc}}"
teleport_owner: "{{cops_mltrainer_medias_owner}}"
teleport_group: "{{cops_mltrainer_medias_group}}"
teleport_use_host: "{{ansible_distribution != 'MacOSX'}}"
teleport_sync_db_files_default:
  dump:
    origin_path: "{{teleport_paths.origin_dump}}/"
    container: "{{teleport_db_container}}"
    container_path: "{{teleport_paths.dest_dump}}/"
    # only_if_exists: true
teleport_sync_files_default:
  medias:
    origin_path: "{{teleport_paths.origin_medias}}/"
    container: "{{teleport_app_container}}"
    container_path: "{{teleport_paths.dest_medias}}/"
    # only_if_exists: true
teleport_sync_db_files: "{{teleport_sync_db_files_default}}"
teleport_sync_files: "{{teleport_sync_files_default}}"
teleport_ssh_key_path: "{{teleport_wd}}/local/.ssh/deploy"
teleport_ssh_origin_key_path: "{{(
  teleport_ssh_key_path or
    hostvars[groups[teleport_origin][0]].get('ansible_ssh_private_key_file', ''))}}"
teleport_ssh_destination_key_path: "{{(
  teleport_ssh_key_path or
    hostvars[groups[teleport_destination][0]].get(
      'ansible_ssh_private_key_file', ''))}}"
teleport_sshkey_stripper: "-i [^ ]+"
teleport_origin_sshargs_withoutkey: "{{teleport_origin_sshargs_default|regex_replace( teleport_sshkey_stripper, '')}}"
teleport_destination_sshargs_withoutkey: "{{teleport_destination_sshargs_default|regex_replace( teleport_sshkey_stripper, '')}}"
# as we connect not from bastion, dont use by default the key version one
teleport_origin_sshargs: "{{teleport_origin_sshargs_withoutkey}}"
teleport_sshargs: "{{teleport_origin_sshargs}}"
teleport_destination_sshargs:  "{{teleport_destination_sshargs_withoutkey}}"
teleport_origin_dump_scripts_postgres: |-
  {% if not teleport_origin_db_is_docker %}
  cd "{{teleport_paths.origin_dump}}"
  {% if teleport_origin_db_password %}PGPASSWORD="{{teleport_origin_db_password}}"{% endif %} \
  pg_dump \
    --host="{{teleport_origin_db_host}}" \
    --port="{{teleport_origin_db_port}}" \
    --username="{{teleport_origin_db_user}}" \
    --dbname="{{teleport_origin_db_name}}" \
    -Fc > {{teleport_paths.origin_dump}}/dump
  {% else %}
  cd "{{teleport_wd}}"
  {{teleport_dc}} exec -T {{teleport_db_container}} pg_dump \
    --username="{{teleport_origin_db_user}}" \
    --dbname="{{teleport_origin_db_name}}" \
    -Fc > {{teleport_paths.origin_dump}}/dump
  {% endif %}
teleport_origin_dump_script_compress: |-
  gzip -c {{teleport_paths.origin_dump}}/dump \
  > "{{teleport_paths_origin_dump_file}}"
  rm -fv {{teleport_paths.origin_dump}}/dump
teleport_origin_dump_script: |-
  set -ex
  {{lookup('vars', 'teleport_origin_dump_scripts_'+teleport_db_mode)}}
  {{teleport_origin_dump_script_compress}}
teleport_origin_db_host: "{{cops_mltrainer_db_host}}"
teleport_origin_db_port: "{{cops_mltrainer_db_password}}"
teleport_origin_db_user: "{{cops_mltrainer_db_user}}"
teleport_origin_db_password: "{{cops_mltrainer_db_password}}"
teleport_origin_db_name: "{{cops_mltrainer_db_name}}"
## standard is for retrocompat
teleport_origin_db_is_docker: "{{_teleport_mode in ['default', 'standard']}}"
teleport_origin_db_dockername: "{{cops_mltrainer_name}}_db_1"
teleport_db_encoding: 'UTF-8'
teleport_pg_restore_drop_db: true
teleport_origin_sshargs_default: >-
  {% set h = groups[teleport_origin][0] %}
  {% set k = teleport_ssh_origin_key_path%}
  {% if k%}-o IdentityFile={{k}} -o IdentitiesOnly=yes{%endif%}
  -o StrictHostKeyChecking=no
  {% if (
      hostvars[h].ansible_connection|default('ssh')
     ) not in [ 'smart', 'local'] %}
  {{ hostvars[h].ssh_args | default('') }}
  {{ hostvars[h].ansible_ssh_args | default('') }}
  {{ hostvars[h].ansible_ssh_common_args | default('') }}
  {{ hostvars[h].ansible_ssh_extra_args | default('') }}
  {% endif %}
  {% if((cops_mltrainer_force_sync_ssh_user) or
        (cops_mltrainer_sync_ssh_user and (cops_mltrainer_force_sync_ssh_user == True))
    )%}
  {% set user = cops_mltrainer_sync_ssh_user or ansible_user or '' %}
  {% if user %}-l {{user}}{% endif %}
  {% endif %}
teleport_destination_sshargs_default: >-
  {% set h = groups[teleport_destination][0] %}
  {% set k = teleport_ssh_destination_key_path%}
  -o StrictHostKeyChecking=no
  {% if k%}-o IdentityFile={{k}} -o IdentitiesOnly=yes{%endif%}
  {% if (
      hostvars[h].ansible_connection|default('ssh')
     ) not in [ 'smart', 'local'] %}
  {{ hostvars[h].ssh_args | default('') }}
  {{ hostvars[h].ansible_ssh_args | default('') }}
  {{ hostvars[h].ansible_ssh_common_args | default('') }}
  {{ hostvars[h].ansible_ssh_extra_args | default('') }}
  {% endif %}
  {% if((cops_mltrainer_force_sync_ssh_user) or
        (cops_mltrainer_sync_ssh_user and (cops_mltrainer_force_sync_ssh_user == True))
    )%}
  {% set user = cops_mltrainer_sync_ssh_user or ansible_user or '' %}
  {% if user %}-l {{user}}{% endif %}
  {% endif %}
teleport_mltrainer_fakeinitial: true
teleport_post_reset_script_default: |-
  set -ex
  {% if teleport_mltrainer_fakeinitial %}
  cd {{teleport_wd}};
  cat|{{cops_mltrainer_drun}} bash -ex - <<EOF
  . /code/venv/bin/activate && \
  python ./manage.py migrate --fake-initial
  EOF
  {%endif%}
  export COMPOSE_INTERACTIVE_NO_CLI=1
teleport_pg_restore_clean: true
teleport_pg_restore_flags: >-
  -U \$POSTGRES_USER -d \$POSTGRES_DB
  --no-owner --no-privileges --no-acl
  {% if teleport_pg_restore_clean%}--clean --if-exists{%endif%}
teleport_pg_restore: |-
  {% if teleport_pg_restore_drop_db%}
  ( LANG=C dropdb -U \$POSTGRES_USER \$POSTGRES_DB 2>&1 || /bin/true; ) \
  | ( egrep -v exist || /bin/true )
  createdb -U \$POSTGRES_USER -O \$POSTGRES_USER -E {{teleport_db_encoding}} \
    \$POSTGRES_DB
  {%endif%}
  zcat {{teleport_paths_dest_dump_file}}|pg_restore {{teleport_pg_restore_flags}}
teleport_reset_db_script_restore_postgres: |-
  set -ex
  cd "{{teleport_wd}}"
  cat|COMPOSE_INTERACTIVE_NO_CLI= {{teleport_dc}} \
  exec -T {{teleport_db_container}} sh -ex - <<EOF
  {{teleport_pg_restore}}
  EOF
teleport_clean_dump: false
teleport_reset_db_script_default: |-
  set -ex
  {{lookup('vars', 'teleport_reset_db_script_restore_'+teleport_db_mode)}}
  {% if teleport_clean_dump %}
  cd "{{teleport_wd}}"
  {{teleport_dc}} \
  exec -T {{teleport_db_container}} rm -f "{{teleport_paths_dest_dump_file}}"
  {% endif %}
teleport_restart_script_default: |-
  set -ex
  cd "{{teleport_wd}}"
  {% for i in teleport_restart_services %}
  {{teleport_dc}} stop {{i}}
  {{teleport_dc}} up -d --force-recreate --no-deps {{i}}
  {% endfor %}
teleport_restart_script: "{{teleport_restart_script_default}}"
teleport_stop_script_default: |-
  set -ex
  cd "{{teleport_wd}}"
  {% for i in teleport_stop_services %}
  {{teleport_dc}} stop {{i}}
  {% endfor %}
teleport_stop_script: "{{teleport_stop_script_default}}"
teleport_reset_db_script: "{{teleport_reset_db_script_default}}"
teleport_post_reset_script: "{{teleport_post_reset_script_default}}"
teleport_db_restart_script: |-
  set -ex
  cd "{{teleport_wd}}"
  {{teleport_dc}} stop {{teleport_db_container}}
  {{teleport_dc}} up -d --force-recreate --no-deps {{teleport_db_container}}
teleport_deploy_ssh_key_paths: |-
  {%- set ret = cops_deploy_ssh_key_paths|copsf_deepcopy %}
  {%- for r, rdata in ret.items() %}
  {%- set _ = rdata.update(
    {'path': teleport_wd+'/local/.ssh/'+r}) %}
  {%- endfor %}
  {{- ret | to_json }}
teleport_dryrun: false
